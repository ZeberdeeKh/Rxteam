-- RX Team — Етап 50 (прибирання мертвого рядка настройки з таблиці settings).
-- Виконати в Supabase → SQL Editor. Ідемпотентно (delete — no-op, якщо рядка вже нема).
--
-- marketplace_flood_hint більше не читається кодом (гілка обговорень усюди жорстко «Zalew»
-- у спільному блоці правил mp_rules) і прибрана з адмінки. Прибираємо й осиротілий рядок,
-- який раніше засівала etap28, щоб у settings не лишалось мертвих ключів.
--
-- rental_stock НЕ чіпаємо: він теж поки не читається кодом, але лишається в адмінці як
-- задум під майбутній ліміт складу оренди — його доля вирішується окремо.

delete from settings where key = 'marketplace_flood_hint';
