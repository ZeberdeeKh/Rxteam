-- RX Team — Етап 35 (карпул: ціна дороги).
-- Виконати в Supabase → SQL Editor ПІСЛЯ etap34.sql. Ідемпотентно.
--
-- Ціна за одне місце (zł), яку водій вказує при реєстрації «своїм ходом». Обов'язкова в коді
-- для нових реєстрацій водіїв; у БД nullable (старі рядки / не-водії = null). Показуємо пасажирам
-- на карпул-мапі для прозорості.
alter table registrations add column if not exists ride_price integer;
