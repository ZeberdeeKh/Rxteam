-- RX Team — Етап 37 (карпул: коментар водія для пасажирів).
-- Виконати в Supabase → SQL Editor ПІСЛЯ etap36.sql. Ідемпотентно.
--
-- Короткий вільний коментар водія (час виїзду, нюанси, «паливо навпіл» тощо), який бачать
-- потенційні пасажири на карпул-мапі та в /drivers. Ліміт довжини — у коді (80 символів).
alter table registrations add column if not exists ride_note text;
