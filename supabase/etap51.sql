-- RX Team — Етап 51 (фіча «Картка гравця» / публічний профіль — прапорець, ВИМКНЕНО за замовч.).
-- Виконати в Supabase → SQL Editor. Ідемпотентно.
--
-- Нова фіча: публічна сторінка /u/<позивний> + клікабельні позивні в «Рейтингу». За конвенцією
-- (нові фічі default-OFF) сідаємо 'false'. Код також гейтує явно на === "true", тож поки прапорець
-- не ввімкнено — сторінки нема (404) і позивні в рейтингу некликабельні (нуль змін на сайті).

insert into settings (key, value) values ('feature_player_card', 'false')
on conflict (key) do nothing;
